version: "3.8"
services:
  app:
    build: .
    volumes:
      - .:/djangoproject
    ports:
      - 8010:8010
    image: app:django
    container_name: django_container
    command: python manage.py runserver 0.0.0.0:8010
